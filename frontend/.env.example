# 前端环境变量
VITE_APP_TITLE=FastAPI Base
# 后端统一挂载在 /api/v1（代码默认值已对齐，可留空）
VITE_API_BASE_URL=/api/v1

# 开发代理目标（vite.config.ts 中 /api 与 /uploads 使用；默认跟随根 .env 的 BACKEND_PORT，
# 仅当后端不在本机时覆盖，如 VITE_PROXY_TARGET=http://192.168.1.10:8000）
# VITE_PROXY_TARGET=http://localhost:8000

# vite dev 端口：由根目录 .env 的 FRONTEND_PORT 统一控制（唯一事实源），不要在此设置
