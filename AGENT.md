# AGENT.md — AI Agent 工作规范

> 本项目（fastapi-base）中 AI coding agent 的**自动执行约定**。
> 目标：无需用户提醒，agent 每次会话自动维护 `.ai/` 工作区，保证工作过程可追溯。

---

## 会话启动（每次开始时）

1. 读取 `.ai/TASKS.md`（任务清单）和 `.ai/LOG.md`（操作日志），恢复上下文
2. 若当前工作对应某任务，确认其计划文档（`.ai/plans/task-XX-*.md`）是否存在、是否需要更新

## 执行边界与沟通约定

1. **未经授权不动手**：用户没有明确说出"开始 / 执行 / 动手"等指令时，**不得自行开始写代码**。此时应输出计划、改动文件清单、决策点，等待用户确认后再实施。
2. **「提交」的语义**：用户说"提交" = 把当前完成的内容形成 **git commit**；**不执行 push**，除非用户明确说"推送 / push"。

## 强制约定（无需用户要求，自动执行）

### 1. 操作日志 `.ai/LOG.md`
- **每轮对话/操作后**自动追加一条日志，**所有对话都要记录，不限代码任务**。覆盖：文件改动、bug 修复、测试执行、git 提交、方案讨论、决策变更、提问回答、验收反馈、复盘确认等一切实质性交互
- 格式：

  | 时间 | 动作 | 涉及文件 | 结果/备注 |
  | --- | --- | --- | --- |

- 引用计划方案时必须标注相对路径：`[计划](./plans/xxx.md)`
- 不要记录敏感信息（密钥、.env 内容、open_id 等）

### 2. 计划方案 `.ai/plans/`
- **动手实现前**先写计划文档（除非任务极小、无方案可言）
- 命名：`task-XX-<slug>.md`（XX 对应 TASKS.md 编号）
- 必备章节：需求 / 现状依据 / 改动文件清单 / 测试计划 / 待确认决策点
- **计划必查两项（缺一不可）**：
  - **数据流闭环**：每个新增字段/功能写明"在哪里填写 → 在哪里消费/展示"（教训：任务一头像填了但右上角展示端没接）
  - **失败路径**：写操作/解析/落库失败如何处理、是否影响主链路、是否提醒用户（教训：任务二审计写入失败需降级策略，不阻塞业务）
- 计划处于"待确认"时标记状态，用户拍板后再标记"已确认"并执行

### 3. 任务清单 `.ai/TASKS.md`
- 新需求/新想法 → 写入对应梯队，补充状态与建议实现
- 任务开始 → 状态标记为"进行中"，并在 LOG 中引用对应计划
- 任务完成 → 更新状态为"已完成"，在 LOG 中记录验证结果

### 4. git 约束
- `.ai/` 目录**永不提交**（已由 .gitignore 忽略），不要把它加入任何 commit
- 其他代码变更遵循项目约定：Conventional Commits、提交前跑对应测试
- **提交与推送分离**：默认只 commit 不 push；只有用户明确要求时才推送

### 5. 复盘与沉淀
- **用户要求复盘时**：把复盘结论（一致点/差异/教训）写入对应任务的计划文档，新增"复盘"章节，并在 LOG 记录
- **任务完成或里程碑时**：可主动询问用户是否需要复盘
- 复盘中提炼的可复用规则（如"数据流闭环检查"）应回写 AGENT.md 或计划模板，沉淀为后续任务默认检查项

## 触发时机汇总

| 时机 | 动作 |
| --- | --- |
| 会话开始 | 读 TASKS.md + LOG.md |
| 每轮对话/操作结束 | 追加 LOG.md（所有对话，不限代码任务） |
| 用户要求复盘 | 复盘结论写入对应 plans + 记 LOG |
| 任务完成/里程碑 | 可主动询问是否复盘 |
| 有新需求 | 写入 TASKS.md |
| 任务开始/结束 | 更新 TASKS.md 状态 + 写/更新 plans + 记 LOG |
| 方案讨论中 | 先写 plans，讨论确认后再动代码 |
| git 操作 | `.ai/` 永不涉及；提交信息用 Conventional Commits |

## 测试与验证基线（改代码后自动执行）

- 后端：`cd backend && PYTHONPATH=. uv run pytest`；ruff：`uv run ruff check app tests scripts`
- 前端：`cd frontend && pnpm test`；lint：`pnpm lint`；构建：`pnpm build`
- 提交前至少跑对应侧测试，结果记入 LOG
